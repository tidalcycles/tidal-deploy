name: Ubuntu

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        ghc: ['9.0.1']
        cabal: ['3.4.0.0']
        
    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Setup Haskell
      uses: haskell/actions/setup@v1
      id: setup-haskell-cabal
      with:
        ghc-version: ${{ matrix.ghc }}
        cabal-version: ${{ matrix.cabal }}
        
    - name: Freeze
      run: |
        cabal freeze
    - name: Cache cabal
      uses: actions/cache@v2
      id: cache-cabal
      with:
        path: '~/.cabal'
        key: ${{ runner.os }}-${{ matrix.ghc }}-${{ hashFiles('~/tidal-deploy/tidal-deploy/src/Main.hs') }}-a
        
    - name: Cache dist
      uses: actions/cache@v2
      id: cache-bin
      with:
        path: '~/tidal-deploy/tidal-deploy/dist-newstyle/'
        key: ${{ runner.os }}-${{ matrix.ghc }}-${{ hashFiles('~/tidal-deploy/tidal-deploy/src/Main.hs') }}-b
    
    - name: cabal update and build
      run: cabal v2-build --enable-tests
    
    - name: remove unneccessary libs 
      run: | 
            ls
            
   # - name: move GHC libs and configs
   #   run: |
            
    - name: move installed packages
      run: ls '~/.cabal/store/ghc-${{ matrix.ghc }}/
   
    #- name: change paths in config files
    #  run: |
   
    #- name: ghc-pkg recache
    #  run: | 
   
   # - name: move executable
   #   run: 
      
    #- name: zip files
    #  run: Compress-Archive -LiteralPath 'D:\a\tidal-deploy\tidal-deploy\binary\' -DestinationPath 'D:\a\tidal-deploy\tidal-deploy\binary.zip'

    #- uses: actions/upload-artifact@v2
    #  with:
    #    name: my-program
    #    path: 'D:\a\tidal-deploy\tidal-deploy\binary.zip'
